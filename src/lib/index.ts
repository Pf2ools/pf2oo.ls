// place files you want to import through the `$lib` alias in this folder.

import { createWindowManager } from "./components/WindowManager.svelte";

export const windowManager = createWindowManager();
